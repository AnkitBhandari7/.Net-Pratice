namespace Week_4;

public class Student
{
    // Instance fields (unique for every object)
    public string name;
    public int age;
    public string address;

    // Static field (shared for all objects)
    public static string collegeName = "London Met University";
}